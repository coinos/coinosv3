node_modules

# Build output (single-file wallet; rebuild with `bun run build`)
/dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*

# Local scratch: E2E harnesses & perf repros (run against regtest)
tools/.*.tmp.js

# Local runtime session store (server side, not part of the wallet build)
/db/
