<?xml version="1.0" encoding="utf-8"?>
<!-- The WebView build: the wallet runs in this app, so this app can wake it.
     See MainActivity for why that trade exists. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />
  <!-- the wake job asks to run when there's a network; the scheduler won't
       take that constraint without this -->
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />
  <application
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:theme="@style/AppTheme"
      android:supportsRtl="true"
      android:usesCleartextTraffic="false">
    <activity
        android:name="io.coinos.app.MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
        android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <!-- the payment schemes the TWA build claims, claimed here too -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="lightning" />
        <data android:scheme="bitcoin" />
        <data android:scheme="nostr" />
        <data android:scheme="lnurl" />
        <data android:scheme="lnurlw" />
        <data android:scheme="lnurlp" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.nfc.action.NDEF_DISCOVERED" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:scheme="lightning" />
        <data android:scheme="bitcoin" />
        <data android:scheme="nostr" />
        <data android:scheme="lnurl" />
        <data android:scheme="lnurlw" />
        <data android:scheme="lnurlp" />
      </intent-filter>
    </activity>
    <!-- the off-screen wallet a push wakes -->
    <service
        android:name="io.coinos.app.WakeJob"
        android:exported="false"
        android:permission="android.permission.BIND_JOB_SERVICE" />
  </application>
</manifest>
